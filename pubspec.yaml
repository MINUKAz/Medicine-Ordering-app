name: untitled3
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.13.0  # Updated dependency
  cloud_firestore: ^5.6.6  # Updated dependency
  firebase_auth: ^5.5.2  # Updated dependency
  file_picker: ^10.0.0  # Updated dependency
  cloudinary: ^1.2.0  # Downgraded dependency for Cloudinary
  flutter_dotenv: ^5.0.2  # Added dependency for dotenv
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/Logo.png